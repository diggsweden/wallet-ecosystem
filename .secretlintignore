# Ignore all certificates and private keys under the config directory
config/**/*.pem
config/**/*.key
config/**/*.p12
config/**/*.csr
config/**/*.crt
config/**/*.cnf
config/**/*.jwt

# Signed LoTE artifacts (generated JWTs for local ecosystem)
config/trust-source/signed/**/*.json
